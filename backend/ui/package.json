{
	"name": "sightline",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "next dev -p 3000",
		"build": "next build",
		"//lint": "No lint script. next lint was removed in Next 16, and every ESLint path for a TypeScript project goes through typescript-eslint, which refuses to load against TypeScript 7 (typescript-eslint#10940). Until that lands, typecheck is the static check: it catches what the type system can see, and the tests cover the rest.",
		"start": "next start",
		"test": "tsc -p tsconfig.test.json && node --test ../../.test-build/**/*.test.js",
		"typecheck": "tsc --noEmit -p tsconfig.json"
	},
	"dependencies": {
		"@databricks/sdk-experimental": "^0.17.0",
		"@databricks/sql": "^2.0.0",
		"@tanstack/react-virtual": "^3.14.9",
		"echarts": "^6.1.0",
		"next": "^16.3.0",
		"pg": "^8.23.0",
		"react": "^19.2.8",
		"react-dom": "^19.2.8",
		"swr": "^2.5.0"
	},
	"devDependencies": {
		"@types/node": "^26.1.2",
		"@types/pg": "^8.23.1",
		"@types/react": "^19.2.18",
		"@types/react-dom": "^19.2.4",
		"typescript": "^7.0.2"
	}
}
